# Product Search MVP

Minimal local MVP for product enrichment, indexing, search, and pipeline experiments.

## Layout

- `compose.yml`: local services
- `opensearch/`: OpenSearch Docker image and config
- `src/`: Python source grouped by responsibility
- `data/`: raw and enriched data
- `products.json`: seed product events
